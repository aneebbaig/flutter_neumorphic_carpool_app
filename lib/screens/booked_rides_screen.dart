import 'package:flutter/material.dart';

class BookedRidesScreen extends StatelessWidget {
  const BookedRidesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Booked Rides");
  }
}
